# Test database — a throwaway DB on the same Docker Postgres (:5433) as dev.
# The test harness refuses to run unless this points at a *_test database, so
# the suite can TRUNCATE freely without ever touching your dev data.
DATABASE_URL="postgresql://jobhunter:jobhunter@localhost:5433/jobhunter_test?schema=public"
